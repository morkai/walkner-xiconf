<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge,chrome=1">
  <meta name="viewport" content="width=device-width, initial-scale=1.0, user-scalable=no">
  <title>Historia - Programator Xitanium</title>
  <link rel="stylesheet" href="/css/main.css">
</head>
<body>
<div id="container">
  <h1 id="hd"><a href="/">Programator Xitanium</a></h1>
  <hr>
  <h2>
    <a href="/history">Historia</a>
    \
    <a href="/history/<%= historyEntry.startedAt.dateString %>"><%= historyEntry.startedAt.dateString %></a>
    \
    <%= historyEntry.id %>
  </h2>
  <dl class="well">
    <dt>Kod 12NC:
    <dd><%= historyEntry.nc %>
    <dt>Start programowania:
    <dd><%= historyEntry.startedAt.dateString %> <%= historyEntry.startedAt.timeString %>
    <dt>Koniec programowania:
    <dd><%= historyEntry.finishedAt.dateString %> <%= historyEntry.finishedAt.timeString %>
    <dt>Wynik programowania:
    <dd><%= historyEntry.error ? 'Porażka' : 'Sukces' %>
    <% if (historyEntry.error) { %>
    <dt>Komunikat błędu:
    <dd><%= historyEntry.error %>
    <% } %>
    <dt>Kod wyjściowy programu programatora:</dt>
    <dd><%= historyEntry.exitCode %></dd>
    <dt>Komunikaty programu programatora:</dt>
    <dd class="profile"><pre><code><%= historyEntry.result %></code></pre>
    <dt>Plik konfiguracyjny programatora:
    <dd class="profile"><pre><code><%= historyEntry.workflow %></code></pre>
    <dt>Plik konfiguracyjny drivera:
    <dd class="profile"><pre><code><%= historyEntry.feature %></code></pre>
  </dl>
</div>
</body>
</html>
