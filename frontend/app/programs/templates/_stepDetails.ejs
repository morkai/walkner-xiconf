<% if (step.enabled) { %>
<% if (step.type === 'pe') { %>
<% include _peDetails.ejs %>
<% } else if (step.type === 'sol') { %>
<% include _solDetails.ejs %>
<% } else if (step.type === 'fn') { %>
<% include _fnDetails.ejs %>
<% } else if (step.type === 'wait') { %>
<% include _waitDetails.ejs %>
<% } %>
<% } %>
